<Page x:Class="DatabaseClient.Pages.GameStatesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:DatabaseClient.Pages"
      mc:Ignorable="d"
      d:DesignHeight="300" d:DesignWidth="300"
      Title="GameStatesPage"
      DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical">
            <Button Margin="5,5,5,5" Click="CreateGamestateButtonClick" Height="30">Create new GameState</Button>
            <Button Margin="5,5,5,5" Click="DeleteGamestateButtonClick" Height="30">Delete selected GameState</Button>
            <Button Margin="5,5,5,5" Click="RefreshButtonClick" Height="30">Refresh</Button>
        </StackPanel>
        <DataGrid Name="GameStateDataGrid" Grid.Column="1" ItemsSource="{Binding GameStates}" AutoGeneratedColumns="GamestateDataGrid_OnAutoGeneratedColumns" CellEditEnding="GamestateDataGrid_OnCellEditEnding" Margin="5" CanUserAddRows="false"/>
    </Grid>
</Page>